# Moustachi Onward — repository handoff
2026-09-19. Highest policy now lives at C:\dev\onward\DEEP_CONTEXT_HANDOFF_FINAL.md.

Source is C:\dev\moustachi-onward, origin happyivanencoding/moustachi-onward main. Core is C:\dev\moustachi, independently maintained. Read README.md, docs/CONTRACTS.md and core docs/OPERATIONS.md before deployment.

src/prompts.mjs retains the source-authoritative JPilot dc5b94c prompt/analytics behavior. src/business.mjs and analytics-aggregate.py perform business evidence reads. src/prepare.mjs exposes prepare/v1. src/worker.mjs uses core submit/get/retry and the unchanged Web feedback lease/outbox contract. It has no ACP client or Runtime session ownership.

Founder Analytics now has four evidence tiers. Summary retains fast tester/invite/funnel counts. Retention reads real per-profile activity history and reports a user as returned today only when retained Analytics shows qualifying activity on an earlier day and again today; it returns bounded tester refs plus exact persisted invite codes and never infers returners by subtracting registrations from active users. Behavior returns current-window cohort labels plus aggregate page foreground-visible time, clicks and bounded per-session paths; when one or a few Txx/invite-code targets are named it also retains per-user page/click detail. Identity is selected only for explicit founder identity/CV questions and returns bounded candidate name/professional context for those targets, with email only when explicitly requested. Named invite-code targeting accepts both the historical `ONWARD + 3 digits` form and the current second-cohort `ONWARD + 4 digits` form; first-cohort registry reporting remains separately limited to its historical 3-digit list. Production retention and targeted behavior paths have both been read directly without exposing raw Profile IDs. Current business regression count is 14/14. Core/native ACP/Runtime tools have separately passed real acceptance.

Production is cut over: `Moustachi Core` and `Moustachi WhatsApp` are Running, and `Moustachi Onward Ops` is enabled with successful periodic no-work cycles. Legacy `Onward Ops Agent` and `Onward WhatsApp Bridge` under `%LOCALAPPDATA%\OnwardOps` are Disabled for rollback. The one-time transport migration completed, preserving/importing 27 rolling context messages, 29 processed IDs and 3 pre-existing outbound receipts while referencing provider-owned WhatsApp auth in place. The new Bridge verified connected/paired/group-configured health and delivered a real outbound smoke.

Business-specific migration scripts remain here rather than in core. `configure-migration.mjs` is first-install only; `migrate-transport.mjs` is one-time and has already run for the live configuration. Do not rerun either script. Keep one Baileys writer per auth directory. No credential copying or safety-check bypass is authorized. Legacy Runtime/Codex native histories are retained, not imported or deleted. Rollback definitions/state are under `%LOCALAPPDATA%\Moustachi\rollback\cutover-20260916-165213`.

Production activation is complete. Onward Control release `20260916T172235-moustachi-cutover` observes the new services; no Onward Web product deploy was required. Future work starts from the live state documented in core Operations. Automated dreaming, broad Personal WhatsApp routing and public connector registration remain separate future work, not Onward Profile responsibilities.
